export interface DesembolsoInterface {
  dpdesndes?: number; // Se permite opcional por si es autogenerado
  dpdesfsol: Date;
  dpdesstat: number;
  dpdesimpt: number;
  dpdesusrs: string;
  dpdesdisp: number;
  dpdesmrcb: number;
}
